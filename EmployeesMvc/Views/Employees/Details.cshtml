@model EmployeesMvc.Models.Employee
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

<!DOCTYPE html>
    <html>
    <head>
        <title>Details</title>
    </head>
    <body>
        <div>
            @Model.Name
            @Model.Email
            @Model.Id
        </div>
    </body>
    </html>
